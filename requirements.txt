# Phase 1 — data pull + spatial join
requests>=2.32
psycopg[binary]>=3.2
python-dotenv>=1.0
geopandas>=1.0
shapely>=2.0
pyarrow>=17.0
sqlalchemy>=2.0          # geopandas.to_postgis() requires SQLAlchemy
geoalchemy2>=0.15        # geopandas → PostGIS geometry types

# Phase 2 — OSRM matrix building (added when Phase 2 starts)
# (no extra deps; uses requests + pyarrow)

# Phase 3 — solver (added when Phase 3 starts)
# ortools>=9.10

# Phase 4 — visualization (added when Phase 4 starts)
# folium>=0.17
# branca>=0.7
