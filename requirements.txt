# Phase 1 — data pull + spatial join
requests>=2.32
psycopg[binary]>=3.2
python-dotenv>=1.0
geopandas>=1.0
shapely>=2.0
pyarrow>=17.0
sqlalchemy>=2.0          # geopandas.to_postgis() requires SQLAlchemy
geoalchemy2>=0.15        # geopandas → PostGIS geometry types

# Phase 2 — OSRM matrix building (uses requests + numpy + pyarrow)
numpy>=1.26

# Phase 3 — solver
ortools>=9.10

# Phase 4 — visualization
folium>=0.17
branca>=0.7
polyline>=2.0            # decode OSRM-encoded route polylines

# Phase 5 — Tier 2 config layer
pyyaml>=6.0              # YAML config loading

# Phase 6 — web app (Tier 2 frontend; doc 05 §5)
fastapi>=0.115           # ASGI framework with Pydantic validation
uvicorn[standard]>=0.30  # ASGI server (the [standard] extra pulls in uvloop + websockets)
jinja2>=3.1              # server-side templating
python-multipart>=0.0.9  # FastAPI Form() body parsing

# Tests
pytest>=8.0
httpx>=0.27              # FastAPI TestClient backend
